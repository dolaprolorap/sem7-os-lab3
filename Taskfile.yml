version: '3.41'

tasks:
  build-run-linux:
    cmds:
      - cmake . -B build && make -C build && ./build/lab3
  build-run-win:
    cmds:
      - cmake . -B build -G "MinGW Makefiles" && cmake --build build && ./build/lab3 
